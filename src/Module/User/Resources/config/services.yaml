imports:
    - { resource: services/repositories.yaml }

services:
    _defaults:
        autowire: true
        autoconfigure: true

    Depense\Module\User\:
        resource: '../../'
        exclude:
            - '../../Resources/'
            - '../../Model/'
            - '../../Repository/'
