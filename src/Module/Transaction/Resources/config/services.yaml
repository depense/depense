imports:
    - { resource: services/repositories.yaml }

services:
    _defaults:
        autowire: true
        autoconfigure: true

    Depense\Module\Transaction\:
        resource: '../../'
        exclude:
            - '../../Resources/'
            - '../../Model/'
            - '../../Repository/'
