imports:
    - { resource: services/repositories.yaml }

services:
    _defaults:
        autowire: true
        autoconfigure: true

    Depense\Module\Taxonomy\:
        resource: '../../'
        exclude:
            - '../../Resources/'
            - '../../Model/'
            - '../../Repository/'
